import { Box } from "theme-ui";

1. Open the Dai UI [sandbox] and create your recipe using the component library.
2. Wrap the component
3. Copy the code into a new file in our [github repo] using the template below and submit a pull request.

<Box p={2} m={2}>

```js
const title = "My Component";
const description = "Add a detailed description.";
const playroomHash = "#?code="; // Add the entire query from the URL.

// Be sure to wrap your component in a backtick like the example.
const component = `
<MyComponent />
`;

export default {
  component,
  title,
  description,
  playroomHash,
};
```

</Box>

[sandbox]: /playroom
[github repo]: https://github.com/makerdao/dai-ui/new/master/packages/web/components/RecipesTemplates
